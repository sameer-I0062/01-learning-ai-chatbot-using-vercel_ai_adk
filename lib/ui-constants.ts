// How close to the bottom (in px) the user must be for new messages to
// auto-scroll the view. Prevents yanking the scroll position if they've
// scrolled up to read earlier messages.
export const AUTO_SCROLL_THRESHOLD_PX = 80;

// How long the "Copied" confirmation shows in a code block's copy button.
export const COPY_FEEDBACK_DURATION_MS = 1500;

// Textarea auto-grow bounds (matches the max-h-40/min-h-11 Tailwind classes).
export const MESSAGE_INPUT_MAX_HEIGHT_PX = 160;
